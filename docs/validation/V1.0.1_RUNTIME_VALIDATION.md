# Better Shift Command v1.0.1 Runtime Validation

Date: 2026-09-15

Source battle log: `script_log_150926_1613.txt` (curated key markers are archived under `runtime_validated_v1.0.1/`).

## Build under test

```text
Controller version=0.2.6
build=BETTER_SHIFT_COMMAND_V1.0.1_TEST_B_V142
Bridge version=0.5.1-per-kind-calibration
MinHook SHA256=e9c9fa622f5220b4dd5162b817b9295c53a7978a7bd0b656eac0d29a1106e8ea
```

The runtime test used the v142-family Bridge build after TESTFIX A's v143 build failed native Hook creation.

## Result A — Bridge hooks/install/start: PASS

Observed:

```text
BRIDGE_OK version=0.5.1-per-kind-calibration
START
REGISTER_LOCAL uid=1001..1004
READY
INPUT_READY
```

No `OBSERVER_MINHOOK_CREATE_FAILED` appeared in the successful TESTFIX B run.

## Result B — pure Move without prior Attack: PASS

The critical marker was:

```text
BRIDGE_ARMED kind=MOVE production_exact_source=false accepted_move=true accepted_attack=false
```

Immediately followed by Controller Move dispatch/ACK. This proves MOVE no longer depends on natural ATTACK calibration.

## Result C — long/multi-unit Move execution: PASS for the v1.0.1 regression

The test contained multiple simultaneously active unit routes and scripted issue IDs continuing beyond 50, with repeated `OWN_MOVE_ACK` and no `CONTROLLER_FAIL reason=OWN_MOVE_PAYLOAD_MISMATCH`.

This directly closes the release blocker exposed by the earlier v1.0.0 long-route trace where a later Move was killed by the 0.05 m payload equality rule.

## Result D — predictive Attack retained: PASS

The successful run still produced several sequences of:

```text
DISPATCH_ATTACK
OWN_ATTACK_ACK
ATTACK_ACCEPTED
```

Therefore the per-kind calibration change did not disable the existing scripted Attack issue path once Attack calibration was available.

## Scope

This run is the **v1.0.1 release-fix regression**, not a replacement for the original HF5 full feature validation. The HF5 archive remains authoritative for the complete Attack-hold/disengagement/Attack-first/player-override feature suite.

## Release conclusion

v1.0.1 release blockers validated:

```text
PASS  native hooks with v142-family build
PASS  MOVE arm with accepted_attack=false
PASS  repeated/long Move ACK chain without old 5 cm hard-fail
PASS  predictive Attack issue/ACK still works
```
